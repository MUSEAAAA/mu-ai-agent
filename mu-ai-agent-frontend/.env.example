# Optional override. Vite loads this value at build time.
# Development defaults to http://localhost:8123/api.
# Production defaults to /api and is proxied by nginx.
VITE_API_BASE_URL=http://localhost:8123/api
